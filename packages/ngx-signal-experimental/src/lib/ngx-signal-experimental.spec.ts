import { ngxSignalExperimental } from './ngx-signal-experimental';

describe('ngxSignalExperimental', () => {
  it('should work', () => {
    expect(ngxSignalExperimental()).toEqual('ngx-signal-experimental');
  });
});
